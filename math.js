// encpsulation

// common js module system(model export and reaquire)

// code moduler maintable and easer to debug.
// import to export 

const add = (a,b) => {
    return a + b ;

}
module.exports = add;
